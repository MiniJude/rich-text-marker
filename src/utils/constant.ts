/**
 * The time difference between the mouse down and mouse up events.
 */
export const MOUSEUP_TIME_DIVIDE = 200;

/**
 * The string that is used to mark the start of the selection.
 */
export const HEAD_TAG_STRING = "data-select_start";

/**
 * The string that is used to mark the end of the selection.
 */
export const TAIL_TAG_STRING = "data-select_end";

/**
 * set default class prefix
 */
export const DEFAULT_CLASS_PREFIX = "rtm-";


/** 
 * set default underline class name
 */
export const DEFAULT_UNDERLINE_CLASS_NAME = DEFAULT_CLASS_PREFIX + "underline";


/**
 * set default mark class prefix
 */
export const DEFAULT_Mark_CLASS_PREFIX = DEFAULT_CLASS_PREFIX + "mark-";